//! TLS 1.3-specific probes: 0-RTT acceptance, ECH advertisement,
//! HelloRetryRequest patterns. Phase 1 stub.

// Re-exported through `scan::protocol::Tls13Result` to keep the JSON
// shape compatible with the spec.
